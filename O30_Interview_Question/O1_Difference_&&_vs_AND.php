<?php

    // $x = true && false; // It will give you false because && has higher priority value then = so first it will comapre both value .

    $x = true and false; // It will give you true beacause priority between '=' or 'and' it gose to '=' so the true assign to = then other thing will dosn't matter


    var_dump($x);


?>